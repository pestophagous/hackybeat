- key: hackybeat
  title: hackybeat
  description:
  fields:
    - name: title
      type: string
      required: true
      description: >
        the stackoverflow question heading

    - name: author
      type: dict
      required: true
      description: >
        the stackoverflow user posting the question

    - name: categories
      type: list
      required: true
      description: >
        the stackoverflow tags on this question
